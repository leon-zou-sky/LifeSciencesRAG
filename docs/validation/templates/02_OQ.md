# 02 · OQ 运行确认（Operational Qualification）

> 证明：标定测量过程受控——阈值不是拍的，是从标准标注集的实测分布推导的。
> 对应需求：REQ-06/07/08/10。

## 2.1 标定输入

| 字段 | 值 |
|---|---|
| 标定集 | config/calibration_pairs.json（{{pairs_count}} 对：正 {{pos_count}} / 负 {{neg_count}}，含难负样本） |
| 标定工具 | scripts/calibrate.py（方案 ②③⑤ 三线互证 + 分布三档判断） |
| 标定执行时间 | {{oq_date}} |

## 2.2 标定输出（工具原始输出归档）

```
{{calibrate_output}}
```

## 2.3 定线决策（人工判断点，审计责任锚点）

| 阈值 | 取值 | 取值理由（引自 thresholds.yaml 注释） |
|---|---|---|
| inquiry.duplicate | {{dup}} | {{dup_reason}} |
| inquiry.new | {{new}} | {{new_reason}} |
| case_report.duplicate / gray | {{case_dup}} / {{case_gray}} | {{case_reason}} |

## 2.4 分布判断

{{distribution_verdict}}

**OQ 结论**：{{oq_conclusion}}
