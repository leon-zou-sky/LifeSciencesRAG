# 01 · IQ 安装确认（Installation Qualification）

> 证明：运行环境组件版本明确、模型权重指纹固定——"系统装在正确的地基上"。
> 本节全部由脚本自动采集生成，采集时间 2026-08-30。

## 1.1 运行环境

| 组件 | 版本/标识 |
|---|---|
| Python | 3.11.14 |
| pymilvus | 3.0.0 |
| sentence-transformers | 5.6.0 |
| Milvus 服务 | 32d5c5b3 |
| MySQL 服务 | 8.0.46 |

## 1.2 模型指纹（防"同名不同物"）

| 字段 | 值 |
|---|---|
| 模型名称 | bge-small-zh-v1.5 |
| 模型路径 | /Users/zouxuefei/agent_code/WeatherAgent/models/bge-small-zh-v1.5 |
| 权重文件 | model.safetensors |
| 权重 SHA-256 | 354763b9b1357bc9c44f62c6be2276321081ed2567773608c0d0785b61d5a026 |
| 向量维度 | 512 |

## 1.3 配置基线（单一事实源快照）

```yaml
# 判重阈值 · 种子文件（2026-08-20 起运行时事实源已下沉 MySQL threshold_config 表，BYOM）
#
# 本文件的角色：① 新环境引导种子（scripts/init_thresholds.py 入库）② DB 不可用时的离线兜底。
# 运行时脚本不读本文件！调参走 scripts/set_threshold.py（强制 --by/--reason 留痕），
# 本文件保持与 DB 同步仅为种子准确性，改本文件不影响运行中系统。
#
# 阈值贴着 embedding 模型的分数分布标定，不是普适常数。
# 换模型 / 数据分布大漂移时必须全量重标：docs/model-selection-recalibration.md（附录 A runbook）

model: bge-small-zh-v1.5   # 本套阈值标定时所用的模型（换模型后此处随重标定更新）

tolerance: 0.03            # 回归漂移容差（与模型分数分布相关，随重标定一起复核）

inquiry:                   # 问询判重：纯向量相似度（question_std vs question_std）
  duplicate: 0.85          #   ≥ 此值：重复，复用/合并（2026-08-17 重标：calibrate 初值 0.80 经回归终审上调——
                           #     灰色探针实测最高 0.8098（"抗生素"泛化问），自动判重线必须压过它，否则人工闸被绕过）
  new: 0.70                #   < 此值：新问题（回归终审：新问探针实测最高 0.6450，灰档探针最低 0.7673，取中间）
                           #   两值之间为灰色带（人工闸）

case_report:               # 病例并案：hybrid = 0.5×向量 + 0.5×四要素规则（药品50%/时间窗30%/患者20%，前置三道硬否决）
  duplicate: 0.87          #   ≥ 此值：并案（status='已合并'，留痕不进影子）
  gray: 0.78               #   < 此值：新病例；两值之间为灰色带（PV 专员人工闸）
                           #   2026-08-19 重锚（case_calibration_pairs.json 14 对分层标定，G-01 关闭）：
                           #   初值 0.90/0.75 为拍脑袋值，实测错 5/14——其中 4 道是规则缺项
                           #   （事件不参与打分/性别不符无否决/药品零重合无护栏），已在 rule_score
                           #   补三道硬否决；否决后分布：merge 0.8995~0.9190 / gray 0.7871~0.8481 /
                           #   new ≤0.7642（被否决对 ≤0.42）。空档 0.8481~0.8995 取 0.87，
                           #   空档 0.7642~0.7871 取 0.78。⚠️ CP-13 余量仅 0.007，样本仍薄，
                           #   badcase 只增不改持续扩充。

generation:                # 生成层（LLM 起草）：资格闸——检索置信不足时 LLM 不出场
  draft_min_top1: 0.60     #   top-K 块最高原始分 < 此值 → insufficient_evidence（管道判拒，非模型判拒）
                           #   2026-08-18 重锚（gate_calibration.json 10 探针实测，替代单样本初值 0.50）：
                           #   full_irrelevant 4 条实测 0.4437~0.5431（用法用量类句式会撞说明书文体，
                           #   向量撞形式不撞内容，0.50 线拦不住——初值单样本是运气）；
                           #   should-pass 层（partial_overlap/adjacent/drug_in_topic_out）实测最低 0.6460。
                           #   空档 0.5431~0.6460 宽 0.10，取 0.60 上下各留 ~0.05 边际。
                           #   设计分工：闸只拦"全书架空"；部分重叠过闸是设计内行为，
                           #   双层兜底（2026-08-19 ③验证实测确认）：
                           #   已收录识别词典的库外药 → coverage_gap 确定性拦截（ANS-05）；
                           #   未收录的 → LLM 拒答点名缺药（ANS-06），prompt 铁律3 兜底。
                           #   缺一半资料的联用问题不起草——答一半有被读成"可以联用"的误读风险
                           #   设立背景：ANS-03 证明 LLM 自判"资料够不够"不可靠（形式引用全合法仍硬答），
                           #   守门责任收归确定性管道，与判重短路同一架构哲学：LLM 只在有资格的场景出场
```

## 1.4 三方版本一致性

| 检查项 | 结果 |
|---|---|
| golden_set.model ↔ thresholds.yaml.model ↔ 模型目录 | ✅ 一致 |

## 1.5 数据规模快照

| Collection | 行数 |
|---|---|
| drug_inserts | 18 |
| guidelines | 9 |
| clinical_papers | 6 |
| case_reports | 16 |
| mi_inquiries | 4 |

**IQ 结论**：✅ 通过
